import dashboard from './dashboard'

const menuItems = {
  // items: [api, profile, dashboard, widget, forms, elements, pages, utilities, support, other]
  items: [dashboard],
}

export default menuItems
